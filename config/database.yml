default: &default
  adapter: mysql2
  encoding: utf8
  reconnect: false
  pool: 5
  username: deploy
  password: deploy
  host: localhost
  # socket: /var/run/mysqld/mysqld.sock

development:
  <<: *default
  database: online_blogs_development

staging:
  <<: *default

production:
  adapter: mysql2
  pool: 5
  encoding: utf8
  reconnect: false
  database: online_blogs_development
  host: localhost
  # host: 127.0.0.1
  username: deploy
  password: deploy

test:
  <<: *default
  database: online_blogs_test

